spring:
  application:
    name: User-Microservice

  datasource:
    url: jdbc:postgresql://localhost:5432/Fitness
    username: postgres
    password: 

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081

